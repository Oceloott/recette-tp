<div class="h-screen bg-gray-800 text-white w-64 fixed top-0 left-0">
    <div class="p-4">
        <h2 class="text-xl font-bold text-green-500 ">Marmishlag</h2>
        <br>
        {% if app.user %}
            <p class="text-sm mt-2">Bienvenue, {{ app.user.firstname }} {{ app.user.lastname }}</p>
        {% endif %}
    </div>
    <nav class="mt-4">
        <ul class="space-y-2">
            {% if app.user %}
                {% if is_granted('ROLE_USER') %}

            <li>
                <a href="{{ path('app_home') }}" class="block py-2 px-4 rounded hover:bg-gray-700">
                    Accueil
                </a>
            </li>
            <li>
                <a href="{{path('recipes_list')}}" class="block py-2 px-4 rounded hover:bg-gray-700">
                    Recettes
                </a>
            </li>
            <li>
                <a href="{{path('generate_recipe')}}" class="block py-2 px-4 rounded hover:bg-gray-700">
                    Pas d'idée ?
                </a>
            </li>
            <li>
                <a href="{{path('add_recipe')}}" class="block py-2 px-4 rounded hover:bg-gray-700">
                    Créer recette
                </a>
            </li>
                {% endif %}
                {% endif %}

            {% if app.user %}
                            {% if is_granted('ROLE_USER') %}

                <li>
                    <a href="{{path('app_profil')}}" class="block py-2 px-4 rounded hover:bg-gray-700">
                        Mon Profil
                    </a>
                </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a href="{{ path('app_admin')}}" class="block py-2 px-4 rounded hover:bg-gray-700">
                            Administration
                        </a>
                    </li>
                {% endif %}
                <li>
                    <a href="{{ path('app_logout') }}" class="block py-2 px-4 rounded hover:bg-red-600">
                        Déconnexion
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('app_login') }}" class="block py-2 px-4 rounded hover:bg-blue-600">
                        Connexion
                    </a>
                </li>
            {% endif %}
        </ul>
    </nav>
</div>
