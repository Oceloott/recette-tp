{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container mx-auto px-6 py-10">

    <header class="text-center">
        <h1 class="text-5xl font-extrabold text-gray-800 leading-tight">
            Bienvenue sur <span class="text-green-500">Marmishlag</span>
        </h1>
        <p class="mt-4 text-lg text-gray-600">
            Découvrez des recettes savoureuses, partagez vos créations, et inspirez-vous de la communauté.
        </p>
    </header>

    <section class="mt-10">
        <div class="bg-white shadow-md rounded-lg p-6">
            <h2 class="text-2xl font-bold text-gray-700">À propos</h2>
            <p class="mt-4 text-gray-600">
                Que vous soyez un chef expérimenté ou un débutant, notre site est conçu pour vous aider à explorer,
                apprendre et partager des recettes. Chaque plat est accompagné d'instructions claires pour garantir
                votre succès en cuisine.
            </p>
        </div>
    </section>

        {% if recipes is not empty %}
    <section class="mt-10">
        <div class="bg-white shadow-md rounded-lg p-6">
            <h2 class="text-2xl font-bold text-gray-700 mb-8">Voici une liste de <span class="text-green-500">recette</span> alléatoire</h2>
        <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for recipe in recipes %}
                <li class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
                    <div class="p-4">
                        {% if recipe.image %}
                            <img src="{{ asset('uploads/recipes/' ~ recipe.image) }}" class="w-full h-48 object-cover rounded-t-lg">
                        {% else %}
                            <div class="w-full h-48 bg-gray-200 flex items-center justify-center text-gray-500 rounded-t-lg">
                                Aucune image
                            </div>
                        {% endif %}
                        <h2 class="text-2xl font-semibold text-gray-800 mt-2 mb-2">{{ recipe.title }}</h2>
                        <p class="text-gray-600 mb-4">{{ recipe.description|length > 100 ? recipe.description[:100] ~ '...' : recipe.description }}</p>
                        <a href="{{ path('recipe_show', { id: recipe.id }) }}" 
                           class="inline-block bg-blue-500 text-white text-sm font-medium py-2 px-4 rounded-lg hover:bg-blue-600 transition-colors duration-200">
                            Voir la recette
                        </a>
                    </div>
                </li>
            {% endfor %}
        </ul>
                        </div>
    </section>
    {% else %}
        <p class="text-center text-gray-500">Aucune recette trouvée.</p>
    {% endif %}

</div>
{% endblock %}
